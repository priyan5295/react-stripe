{
  "name": "reactvite-ecommerce",
  "version": "1.0.0",
  "main": "server.cjs",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "nodemon server.cjs",
    "build": "npm install && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "node server.cjs"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "stripe": "^15.8.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.2"
  }
}
